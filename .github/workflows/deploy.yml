name: CI/CD Pipeline

on:
  push:
    branches: [ main ]  # Runs whenever you push to main

jobs:
  deploy:
    runs-on: ubuntu-latest  # This is the GitHub-hosted runner

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Simple Test
        run: echo "Connecting to EC2 at ${{ secrets.EC2_PUBLIC_IP }}"

      # Add your deployment commands here (e.g., SSH and restart docker)
